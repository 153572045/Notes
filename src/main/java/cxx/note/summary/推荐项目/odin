broadway是基于树状调度框架完成业务的调度，chain框架需要用户手动调整节点并发，当节点数量非常多时，节点的编排会变的很难，不仅影响开发效
率，如果节点编排不恰当还会影响整体性能；



将手动编排节点的方式改成基于数据依赖驱动的方式，图引擎没有调度节点，只有业务节点，每个节点仅关注于自己的依赖数据和产出数据，如果自己的依赖数据准备
就绪，那么会立刻执行，执行完毕之后会去检查依赖于自己的那些节点，将它们的依赖数量减一，如果减一之后依赖数为0，那么就启动该算子的执行，以这种方式自动化
最优并发执行