
一、反码的概念：
将原码取反，以四位为例，原码为0111，则反码为1000

二、补码的概念
将反码+1，若原码为0111，则补码的计算为先取反得 1000，再加1得1001

三、内存中的计算
正数以原码表示，而负数以补码表示
原因： 以8个位为例，8位数的模是256，计算机中的计算都是加法没有减法，而减去一个数等于
加上这个数的补数(模减去原数，比如3的补数为256-3=253),因此内存中用原码表示正数，用
补码表示负数(补码其实就是等于补数)

java中都是有符号的，还是以8位为例，正数只能用右边7位表示，当解析到第8位为1时，解析器
就知道这个数是个负数，以补码的形式去处理。
